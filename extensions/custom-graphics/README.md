# Custom graphics extension

Reusable components/utilities for rendering custom graphics across supported websites.

## Scope
- DOM overlay rendering helpers
- Canvas/SVG utility wrappers
- Shared styling primitives for injected UI
